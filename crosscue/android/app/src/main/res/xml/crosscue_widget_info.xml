<?xml version="1.0" encoding="utf-8"?>
<!-- App pushes updates via WorkManager / HomeWidget.updateWidget, so no system
     periodic refresh is needed (updatePeriodMillis=0). -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/crosscue_widget_description"
    android:initialLayout="@layout/crosscue_widget"
    android:minHeight="110dp"
    android:minWidth="110dp"
    android:previewLayout="@layout/crosscue_widget"
    android:resizeMode="horizontal|vertical"
    android:targetCellHeight="2"
    android:targetCellWidth="2"
    android:updatePeriodMillis="0"
    android:widgetCategory="home_screen" />
